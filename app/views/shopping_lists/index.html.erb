<% @shopping_lists.each do |shopping_list| %>
  <div class="index_row clearfix">
    <h2 class="shopping_list_title"><%= link_to shopping_list.title, shopping_list %></h2>
    <p class="shopping_list_sub_title"><%= shopping_list.description %></p>
  </div>
<% end %>

<div class="links">
  <%= link_to "New Shopping List", new_shopping_list_path %>
</div>